@{
    ViewBag.Title = "Courses";
}

@using CourseHelper.External.Models;
@model IEnumerable<IEnumerable<CourseHelper.External.Models.Course>>
<h2>Course you can apply for (plus some others)</h2>

<p>Not all the requirements for all courses have been inputted into the system. Thus, the courses are divided into two types: (1) those that you certainly can apply for with the inputted subjects and marks; (2) those that have no requirements or have not had their requirements inputted yet.</p>
<p>Disclaimer: Confirm the requirements with the link given to the University of Malta's website. We do not make any claims of certainty with regards to these results, and do not purport to speak in the name of the University of Malta.</p>

<table class="table table-bordered">
    <tr>
        <th>Bachelor courses matching subjects chosen</th>
    </tr>
    <tr>
        <th>Name</th>
        <th>Attendance</th>
        <th>Duration</th>
    </tr>
    @foreach (Course item in Model.ElementAt(0))
    //CourseHelper.External.Models.Course item in Model)
    {
        //var item = Model.ToList()[20];
        <tr>
            <td>
                <a href=@(item.URL) target="_blank">@Html.DisplayFor(modelItem => item.Name)</a>
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Attendance)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Duration)
            </td>
        </tr>
    }
</table>  

@*<table class="table table-bordered">
    <tr>
        <th>Masters</th>
    </tr>
    <tr>
        <th>Name</th>
        <th>Attendance</th>
        <th>Duration</th>
    </tr>
    @foreach (Course item in Model.ElementAt(1))
    //CourseHelper.External.Models.Course item in Model)
    {
        //var item = Model.ToList()[20];
        <tr>
            <td>
                <a href=@(item.URL) target="_blank">@Html.DisplayFor(modelItem => item.Name)</a>
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Attendance)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Duration)
            </td>
        </tr>
    }
</table>*@  

@if (Model.ElementAt(1).Count() > 0)
{
<table class="table table-bordered">
    <tr>
        <th>Bachelor courses with not listed requirements</th>
    </tr>
    <tr>
        <th>Name</th>
        <th>Attendance</th>
        <th>Duration</th>
    </tr>
    @foreach (Course item in Model.ElementAt(1))
    //CourseHelper.External.Models.Course item in Model)
    {
        //var item = Model.ToList()[20];
        <tr>
            <td>
                <a href=@(item.URL) target="_blank">@Html.DisplayFor(modelItem => item.Name)</a>
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Attendance)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Duration)
            </td>
        </tr>
    }
</table>  
}